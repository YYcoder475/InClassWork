<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:models="clr-namespace:InClassWork.Models"
             xmlns:viewmodels="clr-namespace:InClassWork.ViewModels"
             x:DataType="viewmodels:UsersListViewModel"
             x:Class="InClassWork.Views.UsersListView"
             Title="UsersListView">

    <Grid RowDefinitions=".13*,.13*,.9*">

        <Grid ColumnDefinitions="7*,3*">
            <Label
                HorizontalOptions="Center"
                VerticalOptions="Center"
                Margin="80,0,0,0"
                TextColor="CadetBlue"
                FontAttributes="Bold"
                Text="Users List"
                FontSize="30"/>
            <Button
                Grid.Column="1"
                HorizontalOptions="Center"
                VerticalOptions="Center"
                Background="Green"
                TextColor="White"
                Text="Add"
                FontSize="20"/>

        </Grid>

        <!-- Find && Filter Users 1:10-->
        <Grid ColumnDefinitions="7*,3*" Grid.Row="1">
            <SearchBar
                Text="{Binding SearchText}"
                SearchCommand="{Binding SearchCommand}"
                Placeholder="find user"/>
            
            <Button
                Grid.Column="1"
                Margin="0,0,0,15"
                Background="Transparent"
                FontFamily="MaterialIcons"
                FontSize="25"
                TextColor="DarkMagenta"
                Text="{Binding UsersFilterButtonText}"
                Command="{Binding ClearFilterCommand}"/>

        </Grid>

        <!-- Users Collection-->
        <CollectionView Grid.Row="2" SelectionMode="None" ItemsSource="{Binding AllUsers}">

            <!--מה מציגים כאשר אין משתמשים-->
            <CollectionView.EmptyView>
                <StackLayout
                    Padding="20"
                    HorizontalOptions="Center"
                    VerticalOptions="Center">
                    <Label
                        HorizontalTextAlignment="Center"
                        TextColor="Gray"
                        Text="Users not found!"
                        FontSize="24" />
                    <Label
                        HorizontalTextAlignment="Center"
                        TextColor="Gray"
                        Text="Pull down to refresh"
                        FontSize="24"
                        Margin="0,0,0,100"/>

                </StackLayout>
            </CollectionView.EmptyView>
        </CollectionView>


    </Grid>
</ContentPage>